# Gmail SMTP credentials
# For Gmail, create an "App password" and use it here (recommended). Do NOT commit real credentials.
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your-app-password
# Optional from address
EMAIL_FROM=no-reply@yourdomain.com
# Port the email server should listen on
PORT=4000
# Frontend: set VITE_EMAIL_ENDPOINT to the full URL of the send-email endpoint, e.g. http://localhost:4000/send-email
# VITE_EMAIL_ENDPOINT=http://localhost:4000/send-email
